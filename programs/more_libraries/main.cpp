#include <iostream>
#include <string>
#include <ctime>
    // same as #include <time.h>
    // for including C libraries, preface name with 'c' and drop ".h"

#include "../someLib.h" // must actually exist, otherwise pre-processing will fail

#include <string> // only included first time

int main(){


    return 0;
}